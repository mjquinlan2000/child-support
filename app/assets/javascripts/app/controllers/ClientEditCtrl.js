angular.module('childSupportApp')
  .controller('ClientEditCtrl', function($scope, $routeParams, $location, Client) {
    console.log($routeParams);
    if ($routeParams.id) {

    } else {

    }
  });